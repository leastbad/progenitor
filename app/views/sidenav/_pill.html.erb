<li class="nav-item <%= "active" if active %>">
  <a href="<%= node.url %>" class="nav-link d-flex align-items-center justify-content-between">
    <span>
      <span class="sidebar-icon"><span class="fas fa-fw fa-<%= node.icon %>"></span></span>
      <span class="sidebar-text"><%= node.label %></span>
    </span>
    <span class="badge badge-md bg-<%= node.background %> text-<%= node.color %>" data-controller="pill" data-pill-tag-value="<%= node.tag %>"></span>
  </a>
</li>
